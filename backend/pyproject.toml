[project]
name = "backend"
version = "0.1.0"
description = "no-scrum backend"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.16.0",
    "fastapi[standard]>=0.115.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "pydantic-settings>=2.7.0",
]
